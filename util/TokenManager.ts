import { nanoid } from "nanoid";

class TokenManager {
  constructor() {

  }
}

class TokenManagerSingleton {
  private static tokenManager: TokenManager | undefined = undefined;

  constructor() {
    if (this.tokenManager)
  }
}